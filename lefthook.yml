pre-commit:
    parallel: true
    commands:
        lint:
            run: pnpm lint
        prettier:
            stage_fixed: true
            glob: "*.{js,ts,jsx,tsx,md,json}"
            run: pnpm prettier:write {staged_files}
