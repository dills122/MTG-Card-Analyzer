# Used by docker-compose.yml for the local MySQL dev service (RDS storage adapter only --
# not needed for the default nedb setup). Copy to .env and adjust if you want, or leave as-is.
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=MtgCardCatalog
MYSQL_PORT=3306
